Create a documentation/ folder at the repo root containing comprehensive markdown documentation that gets future coding agents up to speed with this project — a fork of the AzerothCore Wrath of the Lich King (WotLK) server. The engineer asked for "everything about the project", so cover the full picture: what this server is and which game version it targets, the repo layout and what each top-level directory is for, how to build and run it (worldserver and the realm/login server, CMake, configuration), the overall architecture and the main server modules and systems (their purpose and how they connect), the data layer (database structure, data files, how content is imported and applied), configuration files and key settings, and notable divergences from upstream (the repo history shows a merged Playerbot branch). Read the actual code and the existing docs to ground every claim — the docs must match the real codebase, and flag anything that looks unfinished or broken rather than glossing over it.
Where: documentation/ (a NEW folder at the repo root — do not write anywhere else). Read-only inputs include doc/, src/, apps/, conf/, data/, modules/, env/, var/, bin/, acore.json, acore.sh, CMakeLists.txt.
Done means: documentation/ exists at the repo root with an index page (README.md or index.md) plus topic pages covering the areas above, coherent as a set and factually accurate against the code — a new agent can read the folder cold and know what this project is, how it is built and run, where everything lives, and how the pieces fit together.
Out of scope: changing any non-documentation file (source code, config, ADWs, existing doc/ content), running or building the server, deployment, and any documentation outside documentation/.
