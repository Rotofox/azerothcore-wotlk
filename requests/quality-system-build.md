Implement the "Item Quality + Scaled Random-Enchant System" module exactly as specified in `specs/db8e1683_item-quality-random-enchants.md` — that document is the authoritative, complete design (it is marked ready to build; translate it into a build plan and code, do NOT re-design or re-litigate it). THIS RUN MUST NOT COMMIT, PUSH, PULL, OR OTHERWISE MUTATE GIT IN ANY WAY — no git add, git commit, git push, git pull, git checkout, git reset, git clean, or any other git command that changes repository state. The run is in --no-commit mode: every work product stays in the working tree, uncommitted, when the run ends.
Where: modify `modules/mod-random-enchants/` in place — `src/random_enchants.cpp`, `src/random_enchants.h`, `src/RE_loader.cpp`, `conf/random_enchants.conf.dist`, and `data/sql/db-world/` — plus the new tables and the generator script the spec defines (Section 3 and the build order). Read-only inputs: the spec itself, the schema files under `data/sql/base/db_world/` (item_template, spellitemenchantment_dbc, quest_template, loot tables), and the core files the spec references (ScriptDefines hooks, Item.h enchant slots, DBCStores).
Done means: the module implements the full award flow per the spec — quality roll with the STAY outcome, per-base-quality `mod_re_rates` (28 rows), variant swap via `mod_re_item_variants`, class-keyed stat pools including weapon/jewelry/relic pools, the ilvl-band enchant matrix in `spellitemenchantment_dbc` with tier multipliers, enchant rolls on PROP slots 7-9, no total cap, and the config additions — the new tables and generator exist with seed/starting data as the spec's Section 4 tables specify, the code is coherent valid C++17, and the reviewer confirms the build matches the plan.
Out of scope: anything not in the spec, any change to core `src/server/game/`, the client DBC patch (the spec documents the requirement only), running a full worldserver build, loot-table surgery, and ANY git operation — committing, pushing, pulling, or even staging.
