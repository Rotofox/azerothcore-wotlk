# Plan: Create `documentation/` — onboarding docs for the AzerothCore WotLK Playerbot fork

> **STATUS: implemented** — the `documentation/` set exists and is the source of truth. This spec is
> the historical design record; the concrete counts/lists below (e.g. "seven modules") reflect the
> tree when it was written. For current state see `documentation/README.md`.

## 1. Mission

Create a NEW folder `documentation/` at the repo root (`/home/nexus-user/azerothcore-wotlk/documentation/`) containing a coherent set of markdown pages that get a future coding agent up to speed with this project: a fork of AzerothCore for Wrath of the Lich King (WotLK) private servers, with the Playerbot system merged in.

**Hard constraints (read them twice):**
- WRITE ONLY inside `documentation/`. Do not modify, create, or delete anything else — no source, no configs, no ADWs, no existing `doc/` content, no root files. The repo already has pre-existing dirty/untracked files (`.gitignore` modified, `justfile` untracked, `requests/` untracked, `adws/` untracked) — leave them exactly as they are.
- READ-ONLY inputs: `doc/`, `src/`, `apps/`, `conf/`, `data/`, `modules/`, `env/`, `var/`, `bin/`, `acore.json`, `acore.sh`, `CMakeLists.txt`, and git history.
- DO NOT build, run, extract, or deploy anything. In particular `env/dist/bin/` contains **compiled server binaries and runtime data** — do not execute them; treat them as evidence to document.
- Every claim must be grounded in the actual code/files. Where something looks unfinished or broken, say so explicitly instead of glossing over it (see §4 "Unfinished / broken items to flag").
- Docs must be coherent as a set: an index page + topic pages, cross-linked, factually accurate.

## 2. What this repo is (verified fact base — use this, but re-verify against code while writing)

- **Identity**: AzerothCore WotLK emulator fork. Remote `origin` = `https://github.com/liyunfan1223/azerothcore-wotlk.git`, checked out on branch `Playerbot` (liyunfan1223's Playerbot fork of azerothcore/azerothcore-wotlk). ~16,629 commits. HEAD is local commit `24c4e5664 "initial commit"` (author `daedalus`, dated 2026-08-14) which touched only `apps/installer/includes/os_configs/ubuntu.sh` and `modules/TrackingModule.h`.
- **Game version**: WotLK 3.3.5a (client build 12340) — the whole codebase is the WotLK expansion (see `src/server/game/`, `src/common/` defines; client build is referenced across the codebase; the fork's branch is titled WotLK). `acore.json`: `name: azerothcore-wotlk`, `version: 14.0.0-dev` (this matches upstream's post-ACDB-13.0.0 dev version — NOT a fork divergence), `license: AGPL3`.
- **Client data**: `env/dist/bin/data-version` = `INSTALLED_VERSION=v16`. Written by `apps/installer/includes/functions.sh` (`inst_download_client_data`, `local VERSION=v16`), which downloads `data.zip` from `https://github.com/wowgaming/client-data/releases/download/v16/data.zip` (dbc, maps, mmaps, vmaps, Cameras).
- **The Playerbot story (be precise — this is subtle)**:
  - The git history is liyunfan1223's `Playerbot` branch: upstream AzerothCore master is repeatedly merged in ("Merge branch 'azerothcore:master' into Playerbot" — latest `f67b86df8`, Sep 28 2025, merging upstream `5d443d3cd` from Sep 27 2025, i.e. upstream ~#23037 era). The repo's own `origin/master` is an ancestor (`9fc8e1181`).
  - Playerbot core support is IN git history: `b8567b3f6 "conf for playerbots log"` (added `Appender.Playerbots` / `Logger.playerbots` to `src/server/apps/worldserver/worldserver.conf.dist`), `MOD_PLAYERBOTS` compile macro handling in `modules/CMakeLists.txt`, `GetPlayerbotsDBRevision` / PLAYERBOTS `#ifdef`s in the codebase, `.github/workflows/core-build-playerbots.yml`.
  - The actual `mod-playerbots` module source lives in `modules/mod-playerbots/` in the working tree but is **NOT tracked by git** (see `.gitignore`: `/modules/*` ignored except `*.md`, `*.sh`, `CMakeLists.txt`, `*.h`, `*.cmake`; `git ls-files modules/mod-playerbots | wc -l` = 0). Same for all other `mod-*` dirs. This matches the upstream install flow ("clone the module into modules/").
- **Installed runtime**: `env/dist/` is the installer's install prefix (`conf/dist/config.sh`: `BINPATH="$AC_PATH_ROOT/env/dist"`) and is fully gitignored except `.gitkeep` files. It currently contains a REAL installed+run server: binaries `worldserver` and `authserver` under `env/dist/bin/`, extracted client data v16 (`dbc/`, `maps/`, `mmaps/`, `vmaps/`, `Cameras/`), logs (`Server.log`, `Playerbots.log`, `Errors.log`, `Auth.log`), generated configs in `env/dist/etc/` (`worldserver.conf`, `authserver.conf`, `modules/*.conf` for every module) plus `*.conf.dist` templates, and the startup-scripts package (`run-engine`, `starter`, `service-manager.sh`, `simple-restarter`, `migrate-registry.sh`, `conf.sh.dist`) — these are copies of `apps/startup-scripts/src/*`.
- **Runtime config highlights** (env/dist/etc): `worldserver.conf`: `RealmID = 1`, `WorldServerPort = 8085`, `BindIP = "0.0.0.0"`, `DataDir = "."`, `Updates.EnableDatabases = 7`, `Updates.AutoSetup = 1`; `authserver.conf`: `RealmServerPort = 3724`; `modules/playerbots.conf`: `AiPlayerbot.Enabled = 1`, `AiPlayerbot.RandomBotAccountCount = 240`, `AiPlayerbot.RandomBotGuildCount = 20`, etc.
- **Modules present in `modules/`**: seven module directories, each an INDEPENDENT git clone with its own `.git`, ALL untracked/gitignored in the parent repo (`.gitignore` ignores `/modules/*` except `*.md`, `*.sh`, `CMakeLists.txt`, `*.h`, `*.cmake`): `mod-playerbots` (the big one: ~1,114 source files / ~171,400 lines; based on IKE3's Playerbots per its README; requires this custom core branch), plus `mod-account-mounts`, `mod-ah-bot`, `mod-aoe-loot`, `mod-autobalance` (has `acore-module.json` v2.2.0 metadata), `mod-no-hearthstone-cooldown`, `mod-random-enchants`. NONE has a `CMakeLists.txt` — CMake globs `modules/*` dirs containing `src/`, compiles all sources into the static `modules` lib, and the generated loader calls each module's `Add<mod>Scripts()` from its `src/<name>_loader.cpp` (e.g. `mod-playerbots` -> `src/playerbots_loader.cpp` -> `Addmod_playerbotsScripts()` -> `AddPlayerbotsScripts()`). `modules/CMakeLists.txt` has special handling: `if (SOURCE_MODULE MATCHES "mod-playerbots")` → defines `MOD_PLAYERBOTS` for `database` and `game-interface` targets.
- **mod-playerbots data layer**: it adds its OWN database `playerbots` (see `modules/mod-playerbots/data/sql/playerbots/create/create_mysql.sql` + `base/` tables: `ai_playerbot_texts`, `playerbots_weightscales`, `playerbots_travelnode_path`, `playerbots_custom_strategy`, `playerbots_db_store`, `playerbots_enchants`, `playerbots_speech*`, `playerbots_account_links`, `playerbots_account_type`, `version_db_playerbots`, …) plus `characters` tables (`playerbots_names`, `playerbots_guild_names`, `playerbots_arena_team_names`) and `world` updates.
- **TrackingModule**: `modules/TrackingModule.h` is tracked (added in HEAD commit); `modules/TrackingModule.cpp` is NOT tracked (gitignored) and NOT referenced by any CMake file — the module loader only globs subdirectories of `modules/`, and the file sits at `modules/` root. Its code references `sEventMgr` (does not exist anywhere in `src/` — verify with `grep -rn sEventMgr src/`), uses `GetOption<bool>(...)` without an owner (non-AzerothCore-style config access), and hardcodes `#define SPELL_MULTITRACK 12345 // Replace this with the spell ID`. **This is unfinished/broken WIP code — flag it.**
- **Agent tooling, NOT server code** (mention briefly in repo-layout/divergence pages so future agents aren't confused): `adws/` (SSSF/AI-workflow data, gitignored), `justfile` (SSSF recipes, untracked), `.env`/`.env.sample` (API keys for agent tooling, gitignored), `requests/` (agent docs, untracked).

## 3. Deliverables — pages to create

Create exactly these files under `documentation/` (all relative paths in this section are relative to `documentation/`; cross-link with relative markdown links):

### 3.1 `documentation/README.md` — INDEX (required entry point)
- One-paragraph "what this is": AzerothCore WotLK (3.3.5a) private-server emulator, fork of liyunfan1223's Playerbot branch; two server processes (worldserver, authserver); Playerbot adds player-like bots.
- "Quick facts" table: game version / client build, acore.json version, data version v16, branch, upstream base (~Sep 2025, upstream #23037 era), modules present, DBs used (auth, characters, world, playerbots).
- "How to read this folder": ordered list mapping reader intent → page (e.g. "want to build → build-and-run.md").
- Table of contents with one-line description of each page and links.
- "Status flags" callout pointing at divergences-and-status.md (broken TrackingModule, module sources untracked, dev-status of playerbots).

### 3.2 `documentation/project-overview.md` — what this server is
- AzerothCore lineage (open-source WotLK 3.3.5a emulator, AGPL-3.0; see `LICENSE`, `AUTHORS`, `acore.json`), this fork's lineage: azerothcore/azerothcore-wotlk → liyunfan1223/azerothcore-wotlk `Playerbot` branch → local checkout with a local commit on top.
- What "Playerbot" means: module `mod-playerbots` (liyunfan1223/mod-playerbots, based on IKE3's Playerbots) — randombots that populate the world and alt-bots; commands via `.bot` (verify in `modules/mod-playerbots/src/cs_playerbots.cpp`); config in `modules/mod-playerbots/conf/playerbots.conf.dist`; note README's "This project is still under development".
- Two-server architecture in one paragraph (see architecture.md for details).
- Where the version numbers come from: `acore.json` (14.0.0-dev = upstream ACDB versioning), `data-version` v16 (client-data release tag), revision banner at build time from `src/cmake/genrev.cmake` → `src/common/GitRevision.*` (git hash/date/branch baked at compile).

### 3.3 `documentation/repo-layout.md` — top-level map
Table or list covering every top-level entry, each with 1–3 sentences grounded in what you see:
- `src/` — C++ source: `common/` (shared libs), `server/` (apps + game + database + scripts + shared), `tools/` (extractors + dbimport), `cmake/`, `test/` (googletest).
- `apps/` — the bash "apps": `installer/` (acore.sh dashboard), `bash_shared/`, `startup-scripts/` (run-engine, starter, service-manager), `docker/`, `ci/`, `compiler/`, `codestyle/`, `config-merger/`, `DatabaseSquash/`, `EnumUtils/`, `extractor/`, `Fmt/`, `git_tools/`, `grafana/`, `test-framework/`, `valgrind/`, `whitespace_remover/` (check each dir before describing; keep summaries short).
- `conf/` — build/installer config: `dist/config.cmake` (CMake options), `dist/config.sh` (installer paths: SRCPATH, BUILDPATH=`var/build/obj`, BINPATH=`env/dist`, DATAPATH, ORIGIN_REMOTE), `dist/env.ac`, `dist/env.docker`, `dist/docker-compose.override.yml`.
- `data/` — SQL data (see data-layer.md).
- `modules/` — module loader infra (CMakeLists.txt, ModulesLoader.cpp.in.cmake, TrackingModule.h/.cpp) + the 7 installed modules (gitignored).
- `deps/` — vendored third-party (boost, mysql, openssl, recastnavigation, g3dlite, gsoap, jemalloc, bzip2, zlib, fmt, argon2, gperftools, SFMT, stdfs, threads, utf8cpp, readline, libmpq, jsonpath) + `acore/` (acore's cmake-utils, bash-lib, joiner, mysql-tools).
- `env/` — installed runtime (see §2): `dist/bin` (binaries + client data + logs + data-version), `dist/etc` (generated configs + module configs), `dist/logs`, startup scripts, `user/` (per-user overrides, empty).
- `var/` — build scratch: `build/obj` (BUILDPATH), `ccache/`, `docker/`, `extractors/` (dbc/maps/mmaps/vmaps/Cameras extraction output).
- `bin/` — bash alias scripts (`acore`, `acore-compiler`, `acore-db-export`, `acore-db-pendings`, `acore-import-changelogs`, `acore-installer`) + `README.md` ("Do not implement scripts here").
- Root files: `acore.sh` (dashboard entry → apps/installer/main.sh), `install.sh`, `CMakeLists.txt`, `PreLoad.cmake`, `docker-compose.yml`, `flake.nix` (Nix flake), `justfile` (SSSF agent tooling — NOT server build), `.env*` (agent tooling), `.devcontainer/`, `.github/` (CI workflows incl. fork's `core-build-playerbots.yml`), `doc/` (existing docs: `Logging.md`, `changelog/`), `AUTHORS`, `LICENSE`, `pull_request_template.md`, `.git_commit_template.txt`, `.vscode/`, `adws/` (agent tooling, gitignored), `requests/` (agent docs).
- Note explicitly: there is NO root README.md — `documentation/` fills that gap.

### 3.4 `documentation/build-and-run.md` — build, configure, run
- Prereqs/deps (MySQL, toolchain; see `apps/installer/includes/os_configs/` and `deps/PackageList.txt`).
- Two ways to build: (a) the AC dashboard `./acore.sh` (menu: init, install-deps, compiler, module, client-data, run-worldserver, run-authserver, service-manager — list from `apps/installer/main.sh` menu_items) and (b) manual CMake: configure with options from `conf/dist/config.cmake` (`BUILD_TOOLS`, `SCRIPTS=static|dynamic|none`, `MODULES=static|dynamic|none`, `TOOLS`, `WITH_WARNINGS`, `CMAKE_CXX_STANDARD` default, `APPS_BUILD` world-only/auth-only, etc. — read the file for exact names), build, install (default prefix `env/dist` per config.sh; binaries land in `env/dist/bin`).
- Products: `worldserver` and `authserver` (+ tools `dbimport`, `map_extractor`, `vmap4_extractor`, `vmap4_assembler`, `mmaps_generator` if TOOLS enabled).
- Config generation: worldserver.conf / authserver.conf are copied from `.conf.dist` templates (`src/server/apps/worldserver/worldserver.conf.dist`, `src/server/apps/authserver/authserver.conf.dist`) on first run or by installer; module configs come from `modules/*/conf/*.conf.dist` (see the "Modules config list" block in `modules/CMakeLists.txt`).
- Database setup: `data/sql/create/create_mysql.sql` (CREATE DATABASE), base dumps in `data/sql/base/`, updates applied by `dbimport` (`src/tools/dbimport/`, `dbimport.conf.dist`; see data-layer.md) or `./acore.sh setup-db`.
- Client data: (a) installer downloads `data.zip` v16 (wowgaming/client-data) into the bin dir and writes `data-version`, or (b) extract from a 3.3.5a client using `apps/extractor/extractor.sh` + the tools (map_extractor, vmap4_extractor/assembler, mmaps_generator) — output lands in `var/extractors/` or the DataDir.
- Running: `worldserver` (world/realm game port 8085) and `authserver` (login 3724) — start via `apps/startup-scripts/src/starter` / `run-engine` / `service-manager.sh` / `simple-restarter` (also copied into `env/dist/`), with GDB crash support. DataDir is relative to the working dir (runtime uses `DataDir = "."`).
- Docker alternative: `docker-compose.yml` (ac-database, ac-authserver, ac-worldserver services per upstream convention; `conf/dist/env.docker` + `docker-compose.override.yml` for module mounts — see mod-playerbots README "Docker Installation").
- **Do NOT actually build or run anything** — document the flow from files.

### 3.5 `documentation/architecture.md` — server architecture
Ground each claim in `src/`. Cover:
- Two processes & how they connect: `authserver` (src/server/apps/authserver: Authentication/, Server/) handles login, session keys, realm list; `worldserver` (src/server/apps/worldserver: Main.cpp, ACSoap/, RemoteAccess/, CommandLine/) runs the game world; client connects to authserver (3724) then to worldserver (8085).
- Shared foundation `src/common/`: Asio (networking), Configuration (sConfigMgr), Cryptography, DataStores (DBC loading), Logging (log4j-like, see doc/Logging.md), Metric, Navigation (mmaps), Collision (vmaps), Threading, IPLocation, Encoding, Utilities. One line each.
- `src/server/shared/` (Realm/… whatever is actually there — check), `src/server/database/` (Database/ workers + prepared statements, Logging/, Updater/) — the three/four DB handles (LoginDatabase, CharacterDatabase, WorldDatabase + playerbots DB from module).
- Game server `src/server/game/` — the core systems, each with a sentence from real headers:
  - World/ (World, WorldConfig, WorldState — game loop, world tick)
  - Maps/ + Grids/ (Map, MapMgr, MapInstanced, MapUpdater, TransportMgr; grid cells, grid loading, terrain)
  - Entities/ (Object, Unit, Player, Creature, GameObject, Item inheritance)
  - AI/ (CreatureAI, ScriptedAI, SmartScripts/SmartAI, CoreAI)
  - Scripting/ (ScriptMgr — the hook/event system scripts and modules register into; ScriptObject)
  - Modules/ (ModuleMgr — module registry)
  - Handlers/ (network CMSG packet handlers: AuthHandler, CharacterHandler, MovementHandler, ChatHandler, …)
  - Spells/ (Spell, SpellMgr, SpellInfo, SpellEffects, Auras)
  - Movement/ (MotionMaster, MovementGenerator, Spline, Waypoints)
  - Battlegrounds/, Instances/, OutdoorPvP/, DungeonFinding/ (LFG), Guilds/, Groups/, AuctionHouse/, Loot/, Mails/, Quests/, Reputation/, Calendar/, Warden/ (anti-cheat), Weather/, Addons/, ArenaSpectator/ etc. (brief).
- Content scripts `src/server/scripts/`: per-continent script libraries (EasternKingdoms, Kalimdor, Northrend, Outland, World, Spells, Commands, Custom, Events, OutdoorPvP, Pet) compiled via `ScriptLoader.cpp.in.cmake`; registration pattern `Add<Zone>Scripts()`.
- Module hook mechanism: modules compiled static or dynamic (`SCRIPTS`/`MODULES` options), loader generated from `modules/ModulesLoader.cpp.in.cmake`, `ModuleMgr`/`ScriptMgr` hook registration; `MOD_PLAYERBOTS` special-casing in `modules/CMakeLists.txt`.
- Tools: `src/tools/dbimport` (DB updater), map/vmap/mmap extractors.
- Threading model: World thread + map update threads (MapUpdater), Asio io contexts — keep to what headers actually show.
- Tests: `src/test/` — googletest based (`src/cmake/googletest.cmake`), what's covered, run via `./acore.sh test`.

### 3.6 `documentation/data-layer.md` — databases, SQL, content import
- The four databases: `auth` (accounts, realm list, motd — check `data/sql/base/db_auth/`), `characters` (players, guilds, etc.), `world` (game content: creature/quest/spell/etc. templates), `playerbots` (added by mod-playerbots).
- `data/sql/` layout: `base/` (db_auth, db_characters, db_world — the squashed baseline), `updates/` (db_auth, db_characters, db_world + `pending_db_*` dirs — currently only `create_sql.sh` inside each), `create/` (create_mysql.sql, drop_mysql.sql), `custom/` (gitignored), `archive/` (old squashed dumps), `old/`; `data/sql/base/database-squash.md` explains the squash/import process.
- Update flow: `dbimport` binary (`src/tools/dbimport/Main.cpp` + `dbimport.conf.dist`) applies base + updates; controlled by `Updates.*` settings in worldserver.conf/authserver.conf (`EnableDatabases` bitmask: 1=auth, 2=characters, 4=world — 7=all, `AutoSetup`, `Redundancy`, `AllowRehash`, `CleanDeadRefMaxCount`); update files live in `data/sql/updates/*` and `pending_db_*` get imported (see `.github/workflows/import_pending.yml`).
- Module SQL: each module ships `data/sql/{auth,characters,world}/` (base + updates) plus playerbots' own `playerbots` DB; module `conf.sh.dist`/`include.sh` registers custom SQL paths for the db assembler.
- Client data files (not SQL): `dbc/` (DataStores), `maps/` (map files), `vmaps/` (collision), `mmaps/` (movement), `Cameras/` — produced by extractors or downloaded (data.zip v16); consumed at runtime via `DataDir` (`env/dist/bin`).
- `var/extractors/` holds extractor output; `env/dist/bin` holds the installed copy.

### 3.7 `documentation/configuration.md` — config files & key settings
- Template vs generated: `.conf.dist` templates are tracked (worldserver: `src/server/apps/worldserver/worldserver.conf.dist`; authserver: `src/server/apps/authserver/authserver.conf.dist`; dbimport: `src/tools/dbimport/dbimport.conf.dist`; module confs: `modules/*/conf/*.conf.dist`); generated `.conf` files live in `env/dist/etc/` (runtime) — gitignored.
- Installer/build config: `conf/dist/config.cmake` (CMake switches), `conf/dist/config.sh` (paths, SKIP_MYSQL_INSTALL, DATAPATH), `conf/dist/env.ac` (docker/compiler env: CTYPE=RelWithDebInfo, CSCRIPTS=static, AC_CCACHE=true).
- worldserver.conf key settings (from the .dist, cite lines): `WorldServerPort` 8085, `BindIP`, `RealmID`, `DataDir`, `LogsDir`, `SOAP.*`, `Ra.Port` (3443), `Updates.*`, plus fork addition `Appender.Playerbots` / `Logger.playerbots` (log to Playerbots.log).
- authserver.conf key settings: `RealmServerPort` 3724, `BindIP`, `Updates.*`, LoginREST if present.
- playerbots.conf key settings (from `modules/mod-playerbots/conf/playerbots.conf.dist`): the big sections — GENERAL, PLAYERBOTS (Enabled, MaxBots…), RANDOMBOT-SPECIFIC (account counts, levels, activities, battlegrounds), PREMADE SPECS, SYSTEM SETTINGS (database & connections, chat, logs) — cite actual option names; note runtime values in `env/dist/etc/modules/playerbots.conf` (Enabled=1, RandomBotAccountCount=240, …).
- Other module confs present at runtime: `AutoBalance.conf`, `mod_account_mount.conf`, `mod_ahbot.conf`, `mod_aoe_loot.conf`, `mod_no_hearthstone_cooldown.conf`, `random_enchants.conf` (in `env/dist/etc/modules/`).
- Logging system: log4j-like loggers/appenders (doc/Logging.md; `Logger.*` / `Appender.*` config).

### 3.8 `documentation/modules.md` — the module system and installed modules
- Module system mechanics (VERIFIED for this tree): CMake discovers modules by globbing `modules/*` directories that contain a `src/` subdir (`GetModuleSourceList` in `src/cmake/macros/ConfigureModules.cmake`), compiles all their sources into the static `modules` library (`CollectSourceFiles` in `modules/CMakeLists.txt`), and the generated loader (`modules/ModulesLoader.cpp.in.cmake` -> `AddModulesScripts()`) calls each module's `Add<mod>Scripts()` entry point. NONE of the 7 installed modules has a `CMakeLists.txt`; each exposes a `src/<name>_loader.cpp` (e.g. `playerbots_loader.cpp`, `ah_bot_loader.cpp`, `AB_loader.cpp`, `aoe_loot_loader.cpp`, `NHC_loader.cpp`, `RE_loader.cpp`) plus a mostly-empty `include.sh` stub (0 bytes for most; mod-playerbots 288 B, mod-no-hearthstone-cooldown 340 B). mod-autobalance carries `acore-module.json` (v2.2.0) as metadata only. Build linkage via `MODULES=static|dynamic|none` (`conf/dist/config.cmake`); modules register hooks through ScriptMgr / ModuleMgr. Each module dir is an INDEPENDENT git clone with its own `.git` — all untracked in the parent repo (see .gitignore).
- One section per installed module, each: what it does (from its README/source), config file, SQL it adds, build/API style, and any obvious gaps. Sources: `modules/mod-*/README.md` where present. Verified origins (each from `git -C modules/<mod> remote -v` / `log -1`): mod-account-mounts <- azerothcore/mod-account-mounts (65ea80f, 2025-03-18); mod-ah-bot <- NathanHandley/mod-ah-bot community fork (eb7b34f, 2025-09-27); mod-aoe-loot <- azerothcore (1efc29f, 2025-02-26); mod-autobalance <- azerothcore (8382937, 2025-08-29); mod-no-hearthstone-cooldown <- BytesGalore community fork (832ef5e, 2025-03-07); mod-playerbots <- liyunfan1223/mod-playerbots (c3eecc0d, 2025-09-28); mod-random-enchants <- azerothcore (02a2e0d, 2025-07-24). SQL/behavior notes: mod-ah-bot has NO SQL ("Moved database configuration completely to config", huge `AuctionHouseBot.*` config tree incl. `AdvancedPricing.*`); mod-aoe-loot ships `data/sql/db-world/base/aoe_loot_acore_string.sql`; mod-random-enchants ships `data/sql/db-world/item_enchatment_random_tiers.sql`; mod-autobalance README warns master is "in beta".
- **mod-playerbots deep dive** (largest section):
  - Identity: liyunfan1223/mod-playerbots, based on IKE3's Playerbots, requires this custom core branch; "still under development" per README. Module checkout is at commit c3eecc0d (2025-09-28, "Merge PR #1676 spec-tab-names"), 2,210 commits in the module repo; ~1,114 source files / ~171,400 lines (src/strategy/ alone approx. 1,049 files / ~128,754 lines, per class: deathknight/druid/hunter/mage/paladin/priest/rogue/shaman/warlock/warrior + generic/triggers/values/actions/rpg/dungeons).
  - What it adds: random bots populating the world (RandomPlayerbotMgr), alt-bots (PlayerbotMgr), AI (PlayerbotAI, strategies per class under `src/strategy/<class>/`, dungeons, travel system TravelMgr/TravelNode), guild task manager, chat filter, command server, `.bot` commands (`cs_playerbots.cpp`).
  - Source map: list the main files under `modules/mod-playerbots/src/` and key subdirs (`strategy/`, `factory/`, …) with one-liners.
  - Config: `conf/playerbots.conf.dist` (sections above) + `conf.sh.dist` (SQL paths).
  - Data: `playerbots` DB + characters/world tables (see §3.6).
  - Wiring: loader (`src/playerbots_loader.cpp` -> `Addmod_playerbotsScripts()` -> `AddPlayerbotsScripts()`), `MOD_PLAYERBOTS` compile define applied to `database` + `game-interface` targets (special-case block in `modules/CMakeLists.txt`, added by fork commit 12d41d131 "Big update."), worldserver.conf Playerbots appender, runtime config Enabled=1. Exactly 17 core files are `#ifdef MOD_PLAYERBOTS`-wired, including a dedicated `PlayerbotsDatabase` (`src/server/database/Database/Implementation/PlayerbotsDatabase.cpp/.h`, registered in `DatabaseLoader`), plus hooks in `WorldSession`/`WorldSessionMgr`/`World`/`IWorld`/`Player`/`MotionMaster`/`cs_server.cpp`/`DBUpdater` — list them from `grep -rln MOD_PLAYERBOTS src/`.
  - Performance notes: README claims thousands of bots; `PerformanceMonitor` exists.

### 3.9 `documentation/divergences-and-status.md` — fork divergences, local state, broken/unfinished flags
- Fork lineage & divergence from upstream azerothcore/azerothcore-wotlk: this is liyunfan1223's `Playerbot` branch (WotLK) — core changes to support playerbots (MOD_PLAYERBOTS, playerbots log appender, PLAYERBOTS ifdefs incl. `GetPlayerbotsDBRevision`, `core-build-playerbots.yml` workflow), plus its dev branches (`Testing`, `Playerbot-250911`, `movement-rewrite`, `modify_char_sel`, `fix_*`, `revert_spell_crash` — list from `git branch -a`), repeated upstream merges (last: Sep 27–28 2025, ~#23037 era).
- Local (non-upstream, non-liyunfan) state: HEAD commit `24c4e5664` (TrackingModule.h + ubuntu.sh tweak); installed runtime under `env/dist` (binaries, data v16, generated configs, logs — evidence a server was built & run here); module sources present but gitignored/untracked.
- **Unfinished / broken items — call these out explicitly**:
  1. `modules/TrackingModule.cpp` + `.h` — WIP "multi-gather" module; not compiled (not referenced by any CMake; loader globs only module subdirs; `.cpp` not even tracked); references nonexistent `sEventMgr`; placeholder spell ID 12345; Trinity-style `GetOption` calls. Dead code today.
  2. `mod-playerbots` README: "This project is still under development" — expect rough edges; check for obvious TODO/FIXMEs and cite a couple of concrete ones (search `modules/mod-playerbots/src` for TODO/FIXME).
  3. Pending DB update dirs (`data/sql/updates/pending_db_*`) contain only `create_sql.sh` — no pending files (normal state; the import workflow exists in CI).
  4. Anything else you find that looks half-done (e.g. mismatched config keys between .dist and runtime .conf, empty dirs, commented-out blocks). List with file paths and evidence.
- Agent-tooling files to disambiguate: `adws/`, `justfile`, `.env*`, `requests/` — not part of the emulator.
- "Docs status": what already exists (`doc/Logging.md`, `doc/changelog/`, `modules/how_to_make_a_module.md`) vs what `documentation/` adds.

## 4. Grounding & accuracy rules for the builder

- Before writing each page, READ the primary sources cited for it. Do not trust this plan's fact base blindly — verify each load-bearing claim against the tree (paths/values can drift).
- Prefer citing concrete paths and real option names over generic statements. Where you quote a config value or a commit, make sure it matches the tree.
- If you find the tree contradicts this plan, the TREE wins — write what's true, and note the discrepancy.
- Keep the tone neutral/technical. Use tables and short bullets; each page should be readable in one sitting.
- Cross-link pages (index ↔ topics, related topics ↔ each other).

## 5. Verification checklist (what "done" means)

1. `documentation/` exists at repo root with exactly the 9 files above (README.md + 8 topic pages).
2. `documentation/README.md` links to every other page; all relative links resolve (check with a quick link scan or manual review).
3. Spot-check factual claims against the tree: ports (8085/3724), `acore.json` version, `data-version` v16, `Updates.EnableDatabases = 7`, `AiPlayerbot.Enabled = 1` and `RandomBotAccountCount = 240` in runtime config, modules list, branch/remote facts, TrackingModule dead-code claims.
4. `git status --short` after writing shows ONLY `documentation/` as new (plus the pre-existing `.gitignore` modification and untracked `justfile`, `requests/`, `adws/` — those were already there before this task; do not touch them).
5. Nothing under `doc/`, `src/`, `apps/`, `conf/`, `data/`, `modules/`, `env/`, `var/`, `bin/` was modified (compare `git status` against the pre-existing dirty set).
6. No server was built or run; no binaries executed.

## 6. Out of scope (do not do)

- Changing any non-`documentation/` file; running or building the server; deployment; editing existing `doc/` content; writing docs anywhere other than `documentation/`; committing the work (leave the working tree as-is).

## 7. Recon addendum (verified after plan was drafted — fold into the pages)

- `deps/acore/` is a git **subrepo** (see `deps/acore/.gitrepo`; pins `azerothcore/cmake-utils` commit 1589c53, method merge) — acore's CMake utility macros (`CU_*` in `cmake-utils/utils.cmake`: `CU_SUBDIRLIST`, `CU_SET/ADD/GET_GLOBAL`, `CU_ADD_HOOK`/`CU_RUN_HOOK`), `bash-lib/`, `joiner/`, `mysql-tools/`. Mention in repo-layout.md.
- The last real build of this tree is recorded in `var/build/obj/CMakeCache.txt` (+ `CMakeFiles/3.28.3/`): CMake 3.28.3, compiler **clang/clang++**, `CMAKE_BUILD_TYPE=Release`, `CMAKE_INSTALL_PREFIX=/home/nexus-user/azerothcore-wotlk/env/dist`, `SCRIPTS=static`, `MODULES=static`, `TOOLS_BUILD=none`, `APPS_BUILD=all`, C++20, PCH on. Use this as the known-good configuration example in build-and-run.md. (`conf/dist/config.sh` also defaults the compilers to `/usr/bin/clang` / `/usr/bin/clang++`.)
- `PreLoad.cmake` is currently inert — only a commented-out Windows example for forcing `CMAKE_INSTALL_PREFIX`; it is an extension point. Say so in build-and-run.md.
- Compiler floor: GCC ≥ 8.0 enforced fatally (`src/cmake/compiler/gcc.cmake`, `GCC_EXPECTED_VERSION 8.0.0`); SSE2 forced. Other toolchains supported: clang, MSVC, icc, mingw (`src/cmake/compiler/`).
- CMake default build type is `RelWithDebInfo` if unset; `USE_COREPCH`/`USE_SCRIPTPCH` default ON (disabled by a `NOPCH` build); `BUILD_TESTING` (default 0) fetches googletest and adds lcov/genhtml coverage targets.
- `TOOLS_BUILD` (default `none`) gates the extractor tools (`dbimport`, `map_extractor`, `vmap4_extractor`, `vmap4_assembler`, `mmaps_generator`); maps tools link vendored `mpq`, `zlib`, `Recast`, `g3dlib`; `dbimport` links `database`, `modules`, `scripts`.

## 8. Recon addendum 2 — architecture specifics (verified; fold into architecture.md / build-and-run.md)

- **Game-version anchors (cite these in project-overview.md and architecture.md)**: `src/common/Banner.cpp:38` prints `AzerothCore 3.3.5a  -  www.azerothcore.org`; `data/sql/base/db_auth/build_info.sql` rows `(12340,3,3,5,'a',...)` and `(13930,3,3,5,'a',...)` (accepted client builds, checked by authserver via `sRealmList->GetBuildInfo` in `src/server/shared/Realms/RealmList.cpp`); `data/sql/base/db_auth/realmlist.sql` default `gamebuild` 12340.
- **worldserver startup pipeline** (`src/server/apps/worldserver/Main.cpp` `int main()`): parse CLI (`--config`, `--version`, `--dry-run`, Windows service flags) -> `sConfigMgr` config load -> `AppenderDB` logging -> banner -> OpenSSL threads/PRNG -> PID file -> asio `IoContext` + `ThreadPool` (config `ThreadPool`, default 2) -> `sScriptMgr->SetScriptLoader(AddScripts)` and `SetModulesLoader(AddModulesScripts)` -> `StartDB()` (Login/Character/World DBs) -> realm offline -> `LoadRealmInfo` (realmlist table) -> `sMetric->Initialize` -> `sSecretMgr->Initialize` -> `sWorld->SetInitialWorldSettings()` (DBCs + DB content) -> optional RA acceptor (`Ra.Port` 3443) -> optional SOAP thread (`SOAP.Port` 7878) -> `sWorldSocketMgr.StartWorldNetwork` (8085) -> `FreezeDetector` watchdog (`MaxCoreStuckTime`, default 60 s) -> CLI console thread -> `WorldUpdateLoop()` (sleep to `MinWorldUpdateTime` default 1 ms, then `sWorld->Update(diff)`). App extras: `ACSoap/`, `RemoteAccess/RASession.cpp` (RA telnet admin), `CommandLine/CliRunnable.cpp`.
- **Binary link libraries** (architecture.md/binaries section): `worldserver` links `modules scripts game gsoap readline gperftools` (+ `-pthread -lncurses` on Unix; also `acore-core-interface`); `authserver` links ONLY `shared` (no game code) — see `src/server/apps/CMakeLists.txt`. Both install to `bin/` under the install prefix (i.e. `env/dist/bin` here).
- **authserver model** (`src/server/apps/authserver/Main.cpp`): event-driven — `sAuthSocketMgr.StartNetwork(*ioContext, bindIp, port)` then `ioContext->run()`; asio `steady_timer` workers `KeepDatabaseAliveHandler` (DB ping) and `BanExpiryHandler` (`BanExpiryCheckInterval` default 60 s); realms refresh via `sRealmList->Initialize(ioContext, RealmsStateUpdateDelay)`. Session state machine in `Server/AuthSession.cpp|.h`: `STATUS_CHALLENGE -> STATUS_LOGON_PROOF -> STATUS_AUTHED` handling `AUTH_LOGON_CHALLENGE`/`AUTH_LOGON_PROOF`/`REALM_LIST` (SRP6, TOTP 2FA). authserver links ONLY `shared` (no game code).
- **src/common one-liners to reuse** (from actual headers): Asio (`Acore::Asio` boost::asio wrapper: IoContext, Strand, SteadyTimer, Resolver), Configuration (`sConfigMgr`, loads main conf + module confs `LoadModulesConfigs`, env-var overrides, typed `GetOption<T>`), Cryptography (BigNumber, AES/ARC4/HMAC, Argon2, TOTP, `OpenSSLCrypto::threadsSetup`), DataStores (`DBCFileLoader` low-level .dbc parser; typed `DBCStore<T>` in `src/server/shared/DataStores/`), Logging (`sLog`, log4j-like loggers/appenders, fmt-style `LOG_INFO`), Metric (`sMetric` optional InfluxDB export), Navigation (`dtQueryFilterExt` for Recast/Detour mmaps), Collision (`DynamicTree` GO-LOS, `BoundingIntervalHierarchy` static vmap queries), Debugging (`Errors`, `WheatyExceptionReport`).
- **Grid constants** (cite in architecture.md Maps section): `src/common/Collision/Maps/MapDefines.h` — `MAX_NUMBER_OF_GRIDS 64`, `MAX_NUMBER_OF_CELLS 8`, `SIZE_OF_GRIDS 533.3333f` (shared by game and tools).

## 9. Recon addendum 3 — mod-playerbots integration specifics (verified; fold into modules.md)

- **Core-side integration**: `PlayerbotScript` class with 9 `OnPlayerbot*` hooks declared in `src/server/game/Scripting/ScriptMgr.h` (lines ~117-125 and ~739+): `OnPlayerbotCheckLFGQueue`, `OnPlayerbotCheckKillTask`, `OnPlayerbotCheckPetitionAccount`, `OnPlayerbotCheckUpdatesToSend`, `OnPlayerbotPacketSent`, `OnPlayerbotUpdate`, `OnPlayerbotUpdateSessions`, `OnPlayerbotLogout`, `OnPlayerbotLogoutBots`; implementation in `src/server/game/Scripting/ScriptDefines/PlayerbotsScript.cpp`; hooks consumed in `LFGQueue.cpp`, `Map.cpp`, `PetitionsHandler.cpp`, `Unit.cpp` (verify with grep).
- **Bot commands** (from `modules/mod-playerbots/src/cs_playerbots.cpp`): `.bot`, `.rndbot`, `.bg`, `.gtask`, `.link`/`.unlink`, `.account`, `.playerbots`, `.reset`, `.stack`, `.toggle`, `.debug`, `.pmon`, `.tick`.
- **PlayerbotsDatabase tables touched by the core** (`src/server/database/Database/Implementation/PlayerbotsDatabase.cpp`): `playerbots_custom_strategy`, `playerbots_db_store`, `playerbots_dungeon_suggestion_abbrevation/definition/strategy`, `playerbots_enchants`, `playerbots_equip_cache`, `playerbots_guild_tasks`, `playerbots_item_info_cache`, `playerbots_random_bots` — plus the module's own `playerbots` DB base tables listed in §3.6. Note: `grep PREPARE_STATEMENT` returns 0 in that file — the DB access style there is worth checking if the docs describe prepared statements.
- **Strategy source size breakdown** (modules.md "what it adds"): `src/strategy/` ≈ 128,754 lines / 1,049 files — per class (deathknight/druid/hunter/mage/paladin/priest/rogue/shaman/warlock/warrior) + generic/triggers/values/actions/rpg, `dungeons/` ≈ 7,927 lines, `raids/` ≈ 27,453 lines (naxxramas, ulduar, icecrown, moltencore, aq20, ...); `src/factory/` ≈ 6,690 lines.

## 10. Recon addendum 4 — build-system details (verified against conf/dist/config.cmake and deps/CMakeLists.txt; fold into build-and-run.md / repo-layout.md)

- **Full `conf/dist/config.cmake` option set** (values verified at lines 14-130): `SCRIPTS` none|static|dynamic|minimal-static|minimal-dynamic (default static); `MODULES` none|static|dynamic (default static); `APPS_BUILD` none|all|auth-only|world-only (default all); `TOOLS_BUILD` none|all|db-only|maps-only (default none); `BUILD_TESTING` 0; `USE_SCRIPTPCH`/`USE_COREPCH` 1; `WITH_WARNINGS` 0; `WITH_COREDEBUG` 0; `WITH_PERFTOOLS` 0; `WITHOUT_GIT` 0; `ENABLE_VMAP_CHECKS` 1; `WITH_DYNAMIC_LINKING` 0; `WITH_STRICT_DATABASE_TYPE_CHECKS` 0; `WITHOUT_METRICS` 0; `WITH_DETAILED_METRICS` 0; `WITH_SOURCE_TREE` hierarchical; `CONFIG_ABORT_INCORRECT_OPTIONS` 0; plus per-script `SCRIPTS_*` vars (e.g. `SCRIPTS_EVENTS=default`). `conf/config.cmake` (user overrides) is `include()`d if present.
- **Build flow order** (root CMakeLists): `add_subdirectory(deps)` -> `src/common` -> hook `BEFORE_SRC_LOAD` -> `add_subdirectory(src)` (src adds `genrev`, `server`, and `tools` unless `TOOLS_BUILD=none`) -> if worldserver is built, `add_subdirectory(modules)` -> hook `AFTER_SRC_LOAD`. Modules compile ONLY when worldserver is built. Revision header generated by `src/cmake/genrev.cmake` into `var/build/obj/revision.h`.
- **deps gating** (`deps/CMakeLists.txt`): mysql only when apps/dbimport needed; zlib + g3dlite + recastnavigation only for worldserver/map tools; readline + gsoap + gperftools only for worldserver; bzip2 + libmpq only for map tools. `deps/PackageList.txt` documents vendored licenses/versions (e.g. jemalloc 5.2.1, g3dlite v9.0 hotfix7). System-found: boost (>=1.74 Linux), mysql (>=8.0 via FindMySQL.cmake), openssl, readline, gperftools, stdfs, threads.
